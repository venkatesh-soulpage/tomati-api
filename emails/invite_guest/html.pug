extends layout

block content
  h1 Hi, #{guest.first_name} #{guest.last_name}
  p
    | Tou have been invited to 
    strong #{event.name} 
    | on 
    strong #{venue.name} 
    | (#{venue.address})
    | at #{moment(event.start_time).format('DD/MM/YYYY LT')}.
    | Please Check-In by doing click on the following button
  // Action
  table.body-action(align='center', width='100%', cellpadding='0', cellspacing='0', role='presentation')
    tbody
      tr
        td(align='center')
          //
            Border based button
            https://litmus.com/blog/a-guide-to-bulletproof-buttons-in-email-design
          table(width='100%', border='0', cellspacing='0', cellpadding='0', role='presentation')
            tbody
              tr
                td(align='center')
                  a.f-fallback.button(href=signupUrl, target='_blank') Signup
  
  table.body-action(align='center', width='100%', cellpadding='0', cellspacing='0', role='presentation')
    tbody
      tr
        td(align='center')
          //
            Border based button
            https://litmus.com/blog/a-guide-to-bulletproof-buttons-in-email-design
          table(width='100%', border='0', cellspacing='0', cellpadding='0', role='presentation')
            tbody
              tr
                td(align='center')
                  p Or you can signup using this code:
                  srong(style={'font-size': '2em', 'font-style': 'bold'}) #{guest.code}
  p
    | Thank You,
    br
    | Booze Boss Team.
  p
    strong P.S.
    |  ¿Need help to start using Booze Boss? Check our 
    a(href=supportUrl)   faq
    | . Or answer to this email.
  // Sub copy
  table.body-sub(role='presentation')
    tbody
      tr
        td
          p.f-fallback.sub
            | If your having issues with the button above you can just copy-paste this link into your browser.
          p.f-fallback.sub=signupUrl