extends layout

block content
  h1 Hi, #{guest.first_name} #{guest.last_name}
  p
    | You are invited to 
    strong #{event.name} 
    | at 
    strong #{venue.name} 
    | (#{venue.address})
    | on #{moment(event.start_time).format('DD/MM/YYYY LT')}.
  br
  p
    | To secure your invitation, sign up for a liquid wallet using the link below.
  br 
  p 
    | If you already have an account you can enter the code below in your app to add this event to your calendar.
  // Action
  table.body-action(align='center', width='100%', cellpadding='0', cellspacing='0', role='presentation')
    tbody
      tr
        td(align='center')
          //
            Border based button
            https://litmus.com/blog/a-guide-to-bulletproof-buttons-in-email-design
          table(width='100%', border='0', cellspacing='0', cellpadding='0', role='presentation')
            tbody
              tr
                td(align='center')
                  a.f-fallback.button(href=signupUrl, target='_blank') Signup
  
  table.body-action(align='center', width='100%', cellpadding='0', cellspacing='0', role='presentation')
    tbody
      tr
        td(align='center')
          //
            Border based button
            https://litmus.com/blog/a-guide-to-bulletproof-buttons-in-email-design
          table(width='100%', border='0', cellspacing='0', cellpadding='0', role='presentation')
            tbody
              tr
                td(align='center')
                  p This code is personal and it can only be redeem once:
                  srong(style={'font-size': '2em', 'font-style': 'bold'}) #{guest.code}
  p
    | Thank You,
    br
    | LiquidIntel Support Team.
  p
    |  Need help managing requisitions? Check out our help 
    a(href=supportUrl) articles 
    | or 
    a(href=supportUrl) FAQ 
  // Sub copy
  table.body-sub(role='presentation')
    tbody
      tr
        td
          p.f-fallback.sub
            | If your having issues with the button above you can just copy-paste this link into your browser.
          p.f-fallback.sub=signupUrl